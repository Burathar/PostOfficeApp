<UserControl x:Class="WpfControlLibrary.NumberUpDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="20">
    <Grid Height="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox x:Name="TbNumber" x:FieldModifier="private" Grid.Column="0"
                         Text="0"
                         TextChanged="TbNumber_TextChanged" KeyDown="TxtNum_OnKeyDown"
                         VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Disabled" />
        <StackPanel Orientation="Vertical" Grid.Column="1">
            <Button x:Name="CmdUp" x:FieldModifier="private" Content="˄" Width="20" Height="10" FontSize="12" Padding="0,-4,0,0" Click="CmdUp_Click" />
            <Button x:Name="CmdDown" x:FieldModifier="private" Content="˅" Width="20" Height="10" FontSize="12" Padding="0,-4,0,0" Click="CmdDown_Click" />
        </StackPanel>
    </Grid>
</UserControl>